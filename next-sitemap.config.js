/** @type {import('next-sitemap').IConfig} */
module.exports = {
    siteUrl: 'https://travelling-bag-store.netlify.app',
    generateRobotsTxt: true, // generates robots.txt
    sitemapSize: 7000,       // optional, number of URLs per sitemap
  }
  